# V2.5 通用接入契约与服务化基线验收记录

> 状态：已完成
> Roadmap：`specs/ROADMAP-v2.5.md`
> Spec：`specs/026-general-integration-service-baseline/spec.md`

## 1. 验收目标

确认 V2.5 已把 `face_api` 的通用接入模式、服务边界、错误处理、audit、运行检查和文档入口统一下来，且没有引入新的公开 API、SDK、WMS 专用逻辑或运行时代码改动。

## 2. 文档检查

| 检查项 | 期望 | 结果 |
|---|---|---|
| V2.5 roadmap | 存在并说明范围和不做事项 | 通过 |
| V2.5 spec/plan/tasks/quickstart | 存在并互相一致 | 通过 |
| 通用接入契约 | 能解释三种接入模式 | 通过 |
| README | 指向 V2.5 通用契约 | 通过 |
| 文档索引 | 新接入方能找到 V2.5 入口 | 通过 |
| 季度计划 | 当前基线包含 V2.5 | 通过 |
| specs 索引 | 包含 `026-general-integration-service-baseline` | 通过 |

## 3. 接入模式验收

| 场景 | 期望结论 | 结果 |
|---|---|---|
| 普通 Web 前端 | 必须走业务后端代理，浏览器不持有 `X-API-Key` | 通过 |
| Java / Spring Boot | 后端保存 API Key，代理调用 Face API，自己签发 session/JWT | 通过 |
| Electron / 终端 | 可受控直连，必须有稳定 `terminal_id` 和 audit 证据 | 通过 |
| 本地验收页面 | 只用于本机或内网调试验收 | 通过 |
| WMS 后续接入 | 按通用模式评估，不新增 WMS 专用 API | 通过 |

## 4. 静态验证

路径检查已覆盖：

```text
specs/ROADMAP-v2.5.md
specs/026-general-integration-service-baseline/spec.md
specs/026-general-integration-service-baseline/plan.md
specs/026-general-integration-service-baseline/tasks.md
specs/026-general-integration-service-baseline/quickstart.md
docs/superpowers/specs/2026-06-19-v2.5-general-integration-service-baseline-design.md
docs/04_usage/06_general_integration_contract.md
docs/90_archive/04_acceptance/09_v2.5_acceptance_record.md
```

```powershell
git diff --check
```

结果：通过，无输出。

未完成标记扫描：

```powershell
$scanTerms = @(
  ('TB' + 'D')
  ('TO' + 'DO')
  ('待' + '定')
  ('占' + '位')
)
```

结果：通过，无输出。

代码测试说明：

- 本版本未修改 Python、JavaScript、HTML 或 CSS 运行时代码。
- 不需要运行 unittest 或 compileall。

## 5. 结论

- [x] V2.5 文档基线完成。
- [x] 未修改公开 API。
- [x] 未新增 SDK。
- [x] 未修改 WMS 代码。
- [x] 后续可进入 V2.6 或独立版本做真实接入实现。
