# V1.8 验收记录

> 范围：现场交付与维护闭环  
> 日期：2026-06-09

## 1. 文档与版本基线

- [x] V1.8 已写入季度计划。
- [x] 文档入口已按角色说明。
- [x] 已完成计划的 checkbox 已同步，避免误导当前进度。

## 2. 可维护性拆分

- [x] `api_errors.py` 已承接结构化错误和中文原因。
- [x] `app_config.py` 已承接环境变量解析和启动校验。
- [x] `api_schemas.py` 已承接 Pydantic 请求/响应模型。
- [x] `admin_ops.py` 已承接维护模式、备份和恢复 helper。
- [x] `main.py` 仍保留 FastAPI app、全局单例和路由编排。

## 3. 现场验收和运行状态

- [x] `camera-integration.html` 可以执行服务检查。
- [x] 页面能显示服务、鉴权、推理设备和人脸库状态。
- [x] `admin.html` 可以查看运行状态总览。
- [x] 注册、登录、活体和最近 audit 流程入口仍保留。

## 4. 架构图演示

- [x] `architecture.html` 支持演示模式。
- [x] `architecture.html` 支持流程播放。
- [x] `architecture.html` 支持导出 SVG。
- [x] 架构说明和文档入口已同步页面能力。

## 5. 验证命令

最终提交前执行：

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest discover -s tests -v
git diff --check
Select-String -Path "H:\AI_test\face_api\architecture.html","H:\AI_test\face_api\camera-integration.html","H:\AI_test\face_api\admin.html" -Pattern "cdn|script src|link rel=.*stylesheet|import |require\("
```

执行结果：

- [x] `D:\anaconda3\envs\face_api\python.exe -m unittest discover -s tests -v`：93 个测试通过。
- [x] `git diff --check`：通过。
- [x] 本地 HTML 外部依赖扫描：无 CDN、外部脚本、外部样式、`import` 或 `require`。
- [x] `architecture.html` 内联 JavaScript 语法检查：通过。

## 6. 残余风险

- 本版本不引入完整业务后台。
- 本版本不引入向量数据库。
- 本版本不引入更强活体模型或硬件活体。
- `architecture.html` 是本地静态 HTML 页面，后续接口、启动方式、存储结构或页面入口变化时，需要同步维护节点、连线和讲解内容。
