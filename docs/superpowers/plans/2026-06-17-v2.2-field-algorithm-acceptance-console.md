# face_api V2.2 Field Algorithm Acceptance Console Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build V2.2 as a local field algorithm acceptance console that validates face login, liveness, lightweight anti-spoofing risk, report export, and conservative tuning guidance from one native HTML page.

**Architecture:** Keep `face_api` API-compatible and do not add backend storage or new public endpoints. Add a standalone `acceptance.html` that calls existing routes, keeps API Key only in browser memory, generates JSON/CSV reports locally, and updates docs/spec-kit/architecture so the tool stays part of the documented system.

**Tech Stack:** Python 3.10, FastAPI existing routes, SQLite existing storage, unittest, native HTML/CSS/JavaScript, SVG in `architecture.html`, Windows PowerShell commands.

---

## Scope And Execution Notes

Before implementing this plan, keep the current V2.1 follow-up code review fixes as a separate commit from V2.2. The current dirty files are unrelated to V2.2 implementation and should not be mixed into the feature commit unless the user explicitly asks.

Current user preference: do not ask about local commits during task execution. Only remind the user when the whole version is complete. Any `git commit` commands below are checkpoint suggestions for the final version handoff, not commands to run automatically.

Recommended pre-flight checkpoint for the already-completed V2.1 follow-up fixes, only if the user explicitly asks to commit before V2.2:

```powershell
git add -- README.md app_config.py business_demo/README.md business_demo/app.py business_demo/storage.py docs/04_usage/04_business_integration_v2.md main.py tests/test_app_config.py tests/test_business_demo.py tests/test_main_api.py "Coding Review/code-review-V2.1-2026-06-16-a9bf2b8.md"
git commit -m "fix: address follow-up v2.1 code review findings"
```

Recommended separate checkpoint for the V2.2 design document already written, only if the user explicitly asks to commit before V2.2:

```powershell
git add -- docs/superpowers/specs/2026-06-16-v2.2-field-algorithm-acceptance-console-design.md
git commit -m "docs: design v2.2 field algorithm acceptance console"
```

If these commits are not made before execution, every V2.2 task must avoid staging those pre-existing files unless the task explicitly modifies them. During `/goal` implementation, use `git status --short` as the checkpoint after each task and save commit instructions for the version-complete final summary.

## File Structure

New files:

- `acceptance.html`: single-file local field acceptance console, with native HTML/CSS/JS only.
- `specs/ROADMAP-v2.2.md`: version-level V2.2 roadmap.
- `specs/023-field-algorithm-acceptance-console/spec.md`: user-facing feature specification.
- `specs/023-field-algorithm-acceptance-console/plan.md`: technical implementation plan for spec-kit.
- `specs/023-field-algorithm-acceptance-console/tasks.md`: dependency-ordered implementation checklist.
- `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md`: V2.2 acceptance record and manual test matrix.

Modified files:

- `tests/test_scripts_smoke.py`: static/smoke tests for `acceptance.html`, V2.2 docs, and architecture sync.
- `architecture.html`: add `acceptance.html` and acceptance/report/tuning flow to the visual architecture.
- `specs/README.md`: add V2.2 to the roadmap index and current execution entry.
- `docs/02_product/02_quarterly_plan.md`: set V2.2 as the next algorithm acceptance baseline.
- `README.md`: add `acceptance.html` to local page entry points.
- `docs/04_usage/03_recognition_security_accuracy.md`: document V2.2 field acceptance workflow and tuning guidance.

---

### Task 1: Create V2.2 Spec-Kit Baseline

**Files:**
- Create: `specs/ROADMAP-v2.2.md`
- Create: `specs/023-field-algorithm-acceptance-console/spec.md`
- Create: `specs/023-field-algorithm-acceptance-console/plan.md`
- Create: `specs/023-field-algorithm-acceptance-console/tasks.md`
- Create: `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md`
- Modify: `specs/README.md`
- Modify: `docs/02_product/02_quarterly_plan.md`

- [ ] **Step 1: Inspect current spec-kit structure**

Run:

```powershell
Get-ChildItem specs
Get-Content -Raw specs\README.md
Get-Content -Raw specs\ROADMAP-v2.1.md
Get-Content -Raw docs\02_product\02_quarterly_plan.md
```

Expected:

- `ROADMAP-v2.1.md` exists and is marked completed.
- `specs/022-lightweight-anti-spoofing` exists.
- No `ROADMAP-v2.2.md` or `023-field-algorithm-acceptance-console` directory exists.

- [ ] **Step 2: Create V2.2 roadmap**

Create `specs/ROADMAP-v2.2.md` with:

```markdown
# face_api Roadmap v2.2

> 创建时间：2026-06-17
> 用途：定义 V2.2 现场算法验收与阈值调优台的范围、边界和执行入口
> 状态：已规划

## 1. 版本定位

V2.2 的目标是把 V2.1 已实现的人脸识别、活体和轻量防翻拍风险能力，变成一个现场可操作、可导出报告、可指导调参的本地验收台。

本版本定位为：

> 现场算法验收与阈值调优台。

V2.2 不继续扩大算法本身，不新增后端数据库表，不新增公开 API。它重点解决“当前算法能力完成得怎么样、现场样例结果如何复盘、后续阈值应该往哪个方向看”的问题。

## 2. 子版本范围

| 子版本 | Spec | 主题 | 目标 |
|---|---|---|---|
| V2.2.1 | `specs/023-field-algorithm-acceptance-console` | 验收台页面 | 新建 `acceptance.html`，跑通摄像头预览、测试用户注册/重绑和五类样例采集 |
| V2.2.2 | `specs/023-field-algorithm-acceptance-console` | 报告导出 | 支持 JSON/CSV 报告，不保存图片或连续帧 |
| V2.2.3 | `specs/023-field-algorithm-acceptance-console` | 调参建议 | 输出小白建议和开发/运维阈值方向，不自动改配置 |
| V2.2.4 | `specs/023-field-algorithm-acceptance-console` | 文档和架构同步 | 更新架构图、使用文档、验收记录和季度计划 |

## 3. 已确认决策

- 新建独立 `acceptance.html`。
- 页面直连 `face_api`，适用于本地工作站或受控内网验收。
- API Key 只保存在浏览器内存，不写入报告。
- 测试用户 `user_id` 使用数字或留空，避免违反 `/faces/register` 的整数约束。
- 固定五类样例：真人正脸、打印照片、手机屏幕照片、电脑屏幕照片、手机播放眨眼视频。
- 每类样例默认采集 3 次。
- 每次采集走完整登录链路：login challenge、连续帧提交、`/auth/face-login`。
- 活体失败时记录该次失败原因，不继续调用 `/auth/face-login`。
- 注册/重绑根据 `/system/status` 的注册活体开关决定是否先走 `register` challenge。
- 结果只保存指标和接口结果，不保存原图、缩略图或连续帧。
- 报告支持 JSON 和 CSV。
- 调参建议只给方向，不直接生成最终环境变量值。
- 同步更新 `architecture.html`。

## 4. 明确不做

- 不新增后端数据库表。
- 不新增验收记录 API。
- 不保存图片或视频帧。
- 不自动修改环境变量。
- 不引入前端框架、CDN 或远程依赖。
- 不承诺企业级强活体。

## 5. 推荐执行入口

```text
/goal Implement face_api Roadmap V2.2 - Field Algorithm Acceptance Console
```

实施前先阅读：

```text
docs/superpowers/specs/2026-06-16-v2.2-field-algorithm-acceptance-console-design.md
specs/023-field-algorithm-acceptance-console/spec.md
specs/023-field-algorithm-acceptance-console/plan.md
specs/023-field-algorithm-acceptance-console/tasks.md
```

## 6. 验收总则

- [ ] `acceptance.html` 可打开并显示摄像头预览。
- [ ] 页面可注册或重绑测试用户。
- [ ] 页面包含五类固定样例，每类默认 3 次。
- [ ] 每次样例可记录成功/失败、风险等级、中文原因和关键指标。
- [ ] 页面可下载 JSON 和 CSV 报告。
- [ ] 页面不保存原图、不保存连续帧、不导出 API Key。
- [ ] 页面输出小白建议和开发/运维阈值方向。
- [ ] `architecture.html`、README、识别安全文档和验收记录同步更新。
```

- [ ] **Step 3: Create feature spec**

Create `specs/023-field-algorithm-acceptance-console/spec.md` with:

```markdown
# 功能规格：现场算法验收与阈值调优台

**功能分支**：`023-field-algorithm-acceptance-console`

**创建时间**：2026-06-17

**状态**：已规划

**输入**：用户确认 V2.2 做独立 `acceptance.html`，用于现场验证真人、打印照片、手机屏幕、电脑屏幕和播放视频样例，并导出报告和保守调参建议。

## Clarifications

### Session 2026-06-16

- Q: 核心交付是什么？ → A: 本地 HTML 验收台。
- Q: 页面落点？ → A: 新建独立 `acceptance.html`。
- Q: 结果是否写数据库？ → A: 不写数据库，只在页面展示并下载 JSON/CSV。
- Q: 样例类型？ → A: 固定五类。
- Q: 每类采集次数？ → A: 默认 3 次。
- Q: 是否给阈值建议？ → A: 给保守建议，不自动改配置。
- Q: API Key 如何处理？ → A: 页面输入，只保存在浏览器内存。
- Q: 采集链路？ → A: 完整登录链路。
- Q: 测试用户？ → A: 页面提供注册/重绑入口，`user_id` 使用数字或留空，因为 `/faces/register` 的 `user_id` 是可选整数。
- Q: 报告字段？ → A: 摘要加关键指标。
- Q: 是否保存图片？ → A: 不保存原图或连续帧。
- Q: 建议面向谁？ → A: 默认小白建议，展开开发/运维阈值方向。
- Q: 是否更新架构图？ → A: 更新。
- Q: 样例人脸？ → A: 尽量使用同一个已注册测试用户。

## 用户场景与测试

### 用户故事 1 - 现场人员完成五类样例验收（P1）

现场人员需要在一个页面里完成测试用户准备、摄像头预览、五类样例采集和结果查看。

**独立测试**：打开 `acceptance.html`，确认页面包含 API 配置、摄像头预览、测试用户注册/重绑、五类样例、每类 3 次采集入口。

### 用户故事 2 - 负责人下载验收报告（P1）

负责人需要下载 JSON/CSV 报告，用于给同事展示和后续复盘。

**独立测试**：构造页面结果后下载 JSON/CSV，确认报告包含样例类型、尝试次数、成功失败、风险等级、中文原因、相似度、质量指标和风险原因，且不包含 API Key、原图或连续帧。

### 用户故事 3 - 运维获得保守调参建议（P2）

运维人员需要看到小白建议和展开后的阈值方向，知道先检查环境还是复核阈值。

**独立测试**：构造真人失败、翻拍低风险、全部失败等结果，页面能输出对应中文建议。

## 功能需求

- **FR-001**：系统必须提供独立 `acceptance.html`。
- **FR-002**：页面必须支持 API 地址和 API Key 输入，API Key 不得写入下载报告。
- **FR-003**：页面必须显示实时摄像头预览。
- **FR-004**：页面必须支持测试用户注册或重绑；`user_id` 必须按可选整数发送，留空时发送 `null`。
- **FR-005**：页面必须固定五类样例并默认每类 3 次。
- **FR-006**：每次样例必须走完整登录链路；如果活体 challenge 返回 `passed: false`，页面必须记录该次失败和中文原因，并且不得继续调用 `/auth/face-login`。
- **FR-007**：页面必须展示成功/失败、风险等级、中文原因和关键指标。
- **FR-008**：页面必须支持 JSON 和 CSV 下载。
- **FR-009**：页面不得保存原图、缩略图或连续帧。
- **FR-010**：页面必须输出小白建议和展开后的开发/运维阈值方向。
- **FR-011**：V2.2 必须同步 `architecture.html` 和相关文档。
- **FR-012**：页面必须兼容注册活体开关；当 `/system/status` 显示注册活体开启时，注册/重绑必须先完成 `register` challenge。

## 成功标准

- **SC-001**：现场人员能用一个页面完成五类样例验收。
- **SC-002**：报告覆盖五类样例和每类 3 次结果。
- **SC-003**：报告不包含 API Key、原图、连续帧或 embedding。
- **SC-004**：页面没有外部 CDN、远程脚本或远程样式依赖。
- **SC-005**：架构图和文档能指向新的验收台入口。
```

- [ ] **Step 4: Create spec-kit technical plan**

Create `specs/023-field-algorithm-acceptance-console/plan.md` with:

```markdown
# 实施计划：现场算法验收与阈值调优台

**分支/目录**：`023-field-algorithm-acceptance-console`

**日期**：2026-06-17

## 技术上下文

- 页面：原生 `acceptance.html`，不引入框架或外部依赖。
- API：复用现有 `/health`、`/faces/register`、`/liveness/challenges`、`/liveness/challenges/submit`、`/auth/face-login`。
- 运行状态：通过 `/system/status` 获取注册活体开关和运行摘要。
- 鉴权：页面输入 `X-API-Key`，仅保存在运行时变量。
- CORS：如果页面通过 `http://localhost:8122` 打开，生产模式需要把该来源加入 `FACE_CORS_ORIGINS`。
- 报告：浏览器本地生成 JSON/CSV Blob 下载。
- 测试：`tests/test_scripts_smoke.py` 静态检查 + 浏览器页面加载检查 + 手动摄像头验收。

## 约束

- 不新增后端接口。
- 不新增数据库表。
- 不保存图片或连续帧。
- 不输出 API Key。
- 不引入 CDN。
- 不新增后端 CORS 特例；使用现有 `FACE_CORS_ORIGINS` 配置。

## 文件变更

- 新建 `acceptance.html`。
- 修改 `tests/test_scripts_smoke.py`。
- 修改 `architecture.html`。
- 修改 `README.md`。
- 修改 `docs/04_usage/03_recognition_security_accuracy.md`。
- 修改 `docs/02_product/02_quarterly_plan.md`。
- 新建 `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md`。
```

- [ ] **Step 5: Create spec-kit tasks**

Create `specs/023-field-algorithm-acceptance-console/tasks.md` with:

```markdown
# Tasks: 现场算法验收与阈值调优台

**Input**: `spec.md`, `plan.md`, `docs/superpowers/specs/2026-06-16-v2.2-field-algorithm-acceptance-console-design.md`

**Tests**: 按 TDD 执行；先写 `tests/test_scripts_smoke.py` 静态测试，再实现页面和文档。

## Phase 1: Setup

- [ ] T001 确认 V2.1 follow-up 修复和 V2.2 设计文档已独立提交或明确不纳入本版本提交
- [ ] T002 创建 V2.2 roadmap/spec/plan/tasks/acceptance record
- [ ] T003 更新 `specs/README.md` 和季度计划中的 V2.2 入口

## Phase 2: Page Contract Tests

- [ ] T004 为 `acceptance.html` 增加本地依赖、安全字段、五类样例、报告导出静态测试
- [ ] T005 确认 T004 在页面不存在时失败

## Phase 3: Acceptance Console

- [ ] T006 新建 `acceptance.html` 页面骨架和样式
- [ ] T007 实现摄像头预览和本地截图/连续帧采集
- [ ] T008 实现 API client、运行状态读取和完整登录链路
- [ ] T009 实现测试用户注册/重绑，兼容注册活体开关和整数 `user_id`
- [ ] T010 实现结果汇总、JSON/CSV 导出和建议规则

## Phase 4: Docs And Architecture

- [ ] T011 更新 `architecture.html`
- [ ] T012 更新 README 和识别安全文档
- [ ] T013 完成 V2.2 验收记录

## Phase 5: Verification

- [ ] T014 运行静态测试、全量 unittest、compileall、git diff --check
- [ ] T015 打开 `acceptance.html` 做浏览器加载和手动摄像头冒烟验收
```

- [ ] **Step 6: Create V2.2 acceptance record**

Create `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md` with:

```markdown
# V2.2 现场算法验收与阈值调优台验收记录

## 版本目标

V2.2 提供独立 `acceptance.html`，用于现场验证真人、打印照片、手机屏幕照片、电脑屏幕照片和手机播放眨眼视频样例，并导出 JSON/CSV 报告和保守调参建议。

## 样例矩阵

| 样例类型 | 预期结果 | 第 1 次 | 第 2 次 | 第 3 次 | 结论 |
|---|---|---|---|---|---|
| 真人正脸 | 应正常通过或低风险 | 待验收 | 待验收 | 待验收 | 待验收 |
| 打印照片 | 不应静默低风险成功 | 待验收 | 待验收 | 待验收 | 待验收 |
| 手机屏幕照片 | 不应静默低风险成功 | 待验收 | 待验收 | 待验收 | 待验收 |
| 电脑屏幕照片 | 不应静默低风险成功 | 待验收 | 待验收 | 待验收 | 待验收 |
| 手机播放眨眼视频 | 至少中风险、失败或残余风险说明 | 待验收 | 待验收 | 待验收 | 待验收 |

## 自动验证记录

| 检查项 | 命令或动作 | 结果 |
|---|---|---|
| 静态 smoke | `D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke -v` | 待执行 |
| 全量 unittest | `D:\anaconda3\envs\face_api\python.exe -m unittest discover -s tests -v` | 待执行 |
| 语法编译 | `D:\anaconda3\envs\face_api\python.exe -m compileall -q main.py app_config.py business_demo tests scripts` | 待执行 |
| diff 检查 | `git diff --check` | 待执行 |

## 隐私和安全确认

- [ ] 报告不包含 API Key。
- [ ] 报告不包含原图、缩略图或连续帧。
- [ ] 报告不包含 embedding。
- [ ] 页面没有外部 CDN、script 或 stylesheet。
- [ ] `http://localhost:8122` 验收方式已确认 CORS 配置。
```

- [ ] **Step 7: Update roadmap indexes**

Modify `specs/README.md` and add:

```markdown
- `ROADMAP-v2.2.md` - 现场算法验收与阈值调优台规划
```

Add current execution entry:

```markdown
Roadmap v2.2 建议执行顺序：

1. `023-field-algorithm-acceptance-console` - 现场算法验收与阈值调优台

Roadmap v2.2 已确认主定位为“现场算法验收与阈值调优台”。关键决策包括：

- 新建独立 `acceptance.html`。
- 固定五类样例，每类默认 3 次。
- 走完整登录链路。
- 活体失败时记录失败，不继续 face login。
- 注册/重绑兼容注册活体开关。
- 支持 JSON/CSV 报告下载。
- 不保存原图、连续帧或 API Key。
- 只提供保守调参方向。
```

Modify `docs/02_product/02_quarterly_plan.md` by adding V2.2 as the next planned version:

```markdown
## V2.2 下一步计划

V2.2 主题为“现场算法验收与阈值调优台”。目标是在不新增后端存储和公开接口的前提下，用 `acceptance.html` 完成五类样例现场验收、报告下载和保守调参建议。
```

- [ ] **Step 8: Run focused doc checks**

Run:

```powershell
rg -n "V2\.2|023-field-algorithm-acceptance-console|acceptance.html|现场算法验收" specs docs\02_product docs\90_archive
git diff --check -- specs docs\02_product docs\90_archive
```

Expected:

- V2.2 appears in roadmap, spec, plan, tasks, quarterly plan, and acceptance record.
- `git diff --check` has no output.

- [ ] **Step 9: Record spec-kit baseline checkpoint**

Run:

```powershell
git status --short
```

Expected:

- V2.2 spec-kit files appear as new/modified files.
- Do not commit yet unless the user explicitly asks; save final commit instructions for the version-complete summary.

---

### Task 2: Lock `acceptance.html` Static Contract With Tests

**Files:**
- Modify: `tests/test_scripts_smoke.py`
- Create later: `acceptance.html`

- [ ] **Step 1: Add failing static tests**

Modify `tests/test_scripts_smoke.py` inside `ScriptSmokeTests`:

```python
    def test_acceptance_page_has_local_dependencies_only(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        self.assertIn("现场算法验收", html)
        self.assertIn("navigator.mediaDevices.getUserMedia", html)
        self.assertIn("/liveness/challenges", html)
        self.assertIn("/liveness/challenges/submit", html)
        self.assertIn("/auth/face-login", html)
        self.assertIn("/faces/register", html)
        self.assertNotIn("cdn", html.lower())
        self.assertNotIn("script src", html.lower())
        self.assertNotIn("link rel=\"stylesheet\"", html.lower())
        self.assertNotIn("import ", html)
        self.assertNotIn("require(", html)

    def test_acceptance_page_declares_required_sample_matrix(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        for sample in ["真人正脸", "打印照片", "手机屏幕照片", "电脑屏幕照片", "手机播放眨眼视频"]:
            self.assertIn(sample, html)
        self.assertIn("attemptTarget: 3", html)
        self.assertIn("evaluateSampleAcceptance", html)

    def test_acceptance_page_report_excludes_sensitive_media(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        self.assertIn("downloadJsonReport", html)
        self.assertIn("downloadCsvReport", html)
        self.assertIn("buildReport", html)
        self.assertIn("apiKey: undefined", html)
        self.assertIn("image: undefined", html)
        self.assertIn("frames: undefined", html)
        self.assertNotIn("localStorage", html)
        self.assertNotIn("sessionStorage", html)
        self.assertNotIn("indexedDB", html)
        self.assertNotIn("embedding", html.lower())

    def test_acceptance_page_has_tuning_recommendation_rules(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        for env_name in [
            "FACE_LIVENESS_MIN_BRIGHTNESS_VARIATION",
            "FACE_ANTI_SPOOF_MIN_FRAME_VARIATION",
            "FACE_ANTI_SPOOF_MIN_FRAME_DELTA",
            "FACE_ANTI_SPOOF_MIN_FACE_MOTION",
            "FACE_ANTI_SPOOF_MIN_SHARPNESS_VARIATION",
        ]:
            self.assertIn(env_name, html)
        self.assertIn("小白建议", html)
        self.assertIn("开发/运维阈值方向", html)
```

- [ ] **Step 2: Run the tests and verify failure**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_local_dependencies_only tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_declares_required_sample_matrix tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_excludes_sensitive_media tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_tuning_recommendation_rules -v
```

Expected before `acceptance.html` exists:

```text
FileNotFoundError
```

- [ ] **Step 3: Record red-test checkpoint**

Run:

```powershell
git status --short -- tests/test_scripts_smoke.py
```

Expected:

- `tests/test_scripts_smoke.py` is modified.
- Do not commit red tests in this workflow; continue to Task 3 and make the tests pass.

---

### Task 3: Build `acceptance.html` Page Shell And Static Report Model

**Files:**
- Create: `acceptance.html`
- Modify: `tests/test_scripts_smoke.py`

- [ ] **Step 1: Create the single-file HTML shell**

Create `acceptance.html` with this page skeleton:

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>face_api V2.2 现场算法验收</title>
  <style>
    :root {
      --bg: #f5f7fb;
      --panel: #ffffff;
      --text: #172033;
      --muted: #5d6b82;
      --line: #d9e0ea;
      --primary: #1167b1;
      --ok: #147a3f;
      --warn: #a15c00;
      --bad: #b42318;
      --radius: 8px;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Microsoft YaHei", "Segoe UI", Arial, sans-serif;
      background: var(--bg);
      color: var(--text);
      letter-spacing: 0;
    }
    header {
      padding: 18px 24px;
      border-bottom: 1px solid var(--line);
      background: #ffffff;
    }
    h1 { margin: 0; font-size: 22px; }
    h2 { margin: 0 0 12px; font-size: 16px; }
    main {
      display: grid;
      grid-template-columns: minmax(320px, 420px) minmax(420px, 1fr);
      gap: 16px;
      padding: 16px;
    }
    section {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      padding: 14px;
    }
    .stack { display: grid; gap: 16px; }
    label { display: block; font-size: 13px; color: var(--muted); margin-bottom: 5px; }
    input, button {
      font: inherit;
      border-radius: 6px;
    }
    input {
      width: 100%;
      border: 1px solid var(--line);
      padding: 9px 10px;
      background: #fff;
    }
    button {
      border: 1px solid var(--primary);
      background: var(--primary);
      color: #fff;
      padding: 9px 12px;
      cursor: pointer;
    }
    button.secondary {
      background: #fff;
      color: var(--primary);
    }
    button:disabled {
      opacity: 0.55;
      cursor: not-allowed;
    }
    video {
      width: 100%;
      aspect-ratio: 4 / 3;
      background: #101828;
      border-radius: var(--radius);
      object-fit: cover;
    }
    .grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
    .sample { border: 1px solid var(--line); border-radius: var(--radius); padding: 12px; }
    .muted { color: var(--muted); font-size: 13px; }
    .status { font-weight: 700; }
    .status.low, .status.ok { color: var(--ok); }
    .status.medium, .status.warn { color: var(--warn); }
    .status.high, .status.bad { color: var(--bad); }
    .result-table { width: 100%; border-collapse: collapse; font-size: 13px; }
    .result-table th, .result-table td { border-bottom: 1px solid var(--line); padding: 8px; text-align: left; }
    details { border: 1px solid var(--line); border-radius: var(--radius); padding: 10px; }
    @media (max-width: 920px) {
      main { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <header>
    <h1>face_api V2.2 现场算法验收</h1>
    <p class="muted">用于真人、照片、屏幕和视频翻拍样例的本地验收；不保存原图、连续帧或 API Key。</p>
  </header>
  <main>
    <div class="stack">
      <section aria-label="运行配置">
        <h2>运行配置</h2>
        <label for="apiBaseUrl">API 地址</label>
        <input id="apiBaseUrl" value="http://localhost:8000">
        <label for="apiKey">X-API-Key</label>
        <input id="apiKey" type="password" autocomplete="off" placeholder="只保存在浏览器内存">
        <div class="grid">
          <div>
            <label for="testUserId">测试 user_id</label>
            <input id="testUserId" value="10001" inputmode="numeric">
          </div>
          <div>
            <label for="testUsername">测试 username</label>
            <input id="testUsername" value="Acceptance User">
          </div>
        </div>
        <div class="grid">
          <button id="checkServiceBtn" type="button">检查服务</button>
          <button id="registerUserBtn" type="button" class="secondary">注册/重绑测试用户</button>
        </div>
        <p id="serviceStatus" class="muted">尚未检查服务。</p>
      </section>
      <section aria-label="摄像头预览">
        <h2>摄像头预览</h2>
        <video id="preview" autoplay playsinline muted></video>
        <div class="grid">
          <button id="startCameraBtn" type="button">打开摄像头</button>
          <button id="captureStillBtn" type="button" class="secondary">测试截图</button>
        </div>
        <p id="cameraStatus" class="muted">请先打开摄像头并调整脸部位置。</p>
      </section>
    </div>
    <div class="stack">
      <section aria-label="样例验收">
        <h2>样例验收</h2>
        <div id="sampleGrid" class="grid"></div>
      </section>
      <section aria-label="结果总览">
        <h2>结果总览</h2>
        <div id="summary"></div>
      </section>
      <section aria-label="报告和建议">
        <h2>报告和建议</h2>
        <div class="grid">
          <button id="downloadJsonBtn" type="button">下载 JSON 报告</button>
          <button id="downloadCsvBtn" type="button" class="secondary">下载 CSV 报告</button>
        </div>
        <div id="recommendations"></div>
      </section>
    </div>
  </main>
  <canvas id="captureCanvas" hidden></canvas>
  <script>
    const state = {
      stream: null,
      apiBaseUrl: "",
      apiKey: "",
      testUser: { user_id: null, username: "" },
      serviceStatus: null,
      attemptTarget: 3,
      samples: [
        { id: "real_face", label: "真人正脸", expected: "应正常通过或低风险", attempts: [] },
        { id: "printed_photo", label: "打印照片", expected: "不应静默低风险成功", attempts: [] },
        { id: "phone_screen_photo", label: "手机屏幕照片", expected: "不应静默低风险成功", attempts: [] },
        { id: "desktop_screen_photo", label: "电脑屏幕照片", expected: "不应静默低风险成功", attempts: [] },
        { id: "phone_replay_video", label: "手机播放眨眼视频", expected: "至少中风险、失败或残余风险说明", attempts: [] }
      ]
    };

    function $(id) { return document.getElementById(id); }
    function setText(id, text) { $(id).textContent = text; }
    function parseTestUserId() {
      const raw = $("testUserId").value.trim();
      if (!raw) return null;
      const value = Number.parseInt(raw, 10);
      if (!Number.isInteger(value) || String(value) !== raw) {
        throw new Error("测试 user_id 必须是整数，或留空");
      }
      return value;
    }
    function readConfig() {
      state.apiBaseUrl = $("apiBaseUrl").value.replace(/\/+$/, "");
      state.apiKey = $("apiKey").value;
      state.testUser = {
        user_id: parseTestUserId(),
        username: $("testUsername").value.trim()
      };
    }
    function renderSamples() {
      $("sampleGrid").innerHTML = state.samples.map(sample => `
        <article class="sample">
          <h3>${sample.label}</h3>
          <p class="muted">${sample.expected}</p>
          <p>进度：${sample.attempts.length}/${state.attemptTarget}</p>
          <button type="button" onclick="runSampleAttempt('${sample.id}')">采集 1 次</button>
        </article>
      `).join("");
    }
    function normalizeAttempt(result) {
      return {
        success: Boolean(result.authenticated || result.passed),
        similarity: result.similarity ?? result.match?.similarity ?? null,
        risk_level: result.anti_spoof_risk?.level || "unknown",
        risk_reasons: result.anti_spoof_risk?.reasons || [],
        quality: result.quality_metrics || result.face?.quality_metrics || {},
        message: result.message || result.reason || result.detail?.reason || "无中文原因",
        apiKey: undefined,
        image: undefined,
        frames: undefined
      };
    }
    function evaluateSampleAcceptance(sample, counts) {
      if (sample.id === "real_face") {
        return counts.success > 0 && counts.high === 0;
      }
      const lowRiskSuccess = sample.attempts.some(attempt => attempt.success && attempt.risk_level === "low");
      return !lowRiskSuccess;
    }
    function summarizeSample(sample) {
      const counts = { success: 0, failure: 0, low: 0, medium: 0, high: 0 };
      sample.attempts.forEach(attempt => {
        counts[attempt.success ? "success" : "failure"] += 1;
        if (["low", "medium", "high"].includes(attempt.risk_level)) counts[attempt.risk_level] += 1;
      });
      return {
        success_count: counts.success,
        failure_count: counts.failure,
        low_count: counts.low,
        medium_count: counts.medium,
        high_count: counts.high,
        accepted: evaluateSampleAcceptance(sample, counts)
      };
    }
    function buildReport() {
      readConfig();
      return {
        version: "V2.2",
        generated_at: new Date().toISOString(),
        api_base_url: state.apiBaseUrl,
        test_user: state.testUser,
        samples: state.samples.map(sample => ({
          sample_type: sample.label,
          expected: sample.expected,
          attempts: sample.attempts,
          summary: summarizeSample(sample)
        })),
        recommendations: buildRecommendations(),
        apiKey: undefined,
        image: undefined,
        frames: undefined
      };
    }
    function buildRecommendations() {
      return [
        { audience: "小白建议", text: "真人失败多时，先检查光线、摄像头角度、人脸距离和画面清晰度。" },
        { audience: "小白建议", text: "翻拍样例低风险多时，说明当前轻量防翻拍偏宽松，需要开发或运维复核阈值。" },
        { audience: "开发/运维阈值方向", text: "亮度波动不足时关注 FACE_LIVENESS_MIN_BRIGHTNESS_VARIATION。" },
        { audience: "开发/运维阈值方向", text: "防翻拍亮度变化不足时关注 FACE_ANTI_SPOOF_MIN_FRAME_VARIATION。" },
        { audience: "开发/运维阈值方向", text: "连续帧太像时关注 FACE_ANTI_SPOOF_MIN_FRAME_DELTA。" },
        { audience: "开发/运维阈值方向", text: "人脸框几乎不动时关注 FACE_ANTI_SPOOF_MIN_FACE_MOTION。" },
        { audience: "开发/运维阈值方向", text: "清晰度变化不足时关注 FACE_ANTI_SPOOF_MIN_SHARPNESS_VARIATION。" }
      ];
    }
    function downloadBlob(filename, text, type) {
      const blob = new Blob([text], { type });
      const url = URL.createObjectURL(blob);
      const link = document.createElement("a");
      link.href = url;
      link.download = filename;
      link.click();
      URL.revokeObjectURL(url);
    }
    function downloadJsonReport() {
      downloadBlob("face-api-v2.2-acceptance.json", JSON.stringify(buildReport(), null, 2), "application/json;charset=utf-8");
    }
    function downloadCsvReport() {
      const report = buildReport();
      const rows = [["样例类型","第几次","是否成功","风险等级","相似度","det_score","brightness","sharpness","face_pixels","中文原因","风险原因"]];
      report.samples.forEach(sample => sample.attempts.forEach((attempt, index) => {
        rows.push([
          sample.sample_type,
          String(index + 1),
          attempt.success ? "成功" : "失败",
          attempt.risk_level,
          attempt.similarity ?? "",
          attempt.quality.det_score ?? "",
          attempt.quality.brightness ?? "",
          attempt.quality.sharpness ?? "",
          attempt.quality.face_pixels ?? "",
          attempt.message,
          attempt.risk_reasons.join(";")
        ]);
      }));
      downloadBlob("face-api-v2.2-acceptance.csv", rows.map(row => row.map(cell => `"${String(cell).replace(/"/g, '""')}"`).join(",")).join("\r\n"), "text/csv;charset=utf-8");
    }
    async function runSampleAttempt(sampleId) {
      const sample = state.samples.find(item => item.id === sampleId);
      sample.attempts.push(normalizeAttempt({ message: "页面骨架已记录，API 流程在后续任务接入" }));
      renderSamples();
      renderSummary();
    }
    function renderSummary() {
      $("summary").innerHTML = state.samples.map(sample => {
        const summary = summarizeSample(sample);
        return `<p><strong>${sample.label}</strong>：成功 ${summary.success_count}，失败 ${summary.failure_count}，low ${summary.low_count}，medium ${summary.medium_count}，high ${summary.high_count}</p>`;
      }).join("");
      $("recommendations").innerHTML = buildRecommendations().map(item => `<p><strong>${item.audience}</strong>：${item.text}</p>`).join("");
    }
    $("downloadJsonBtn").addEventListener("click", downloadJsonReport);
    $("downloadCsvBtn").addEventListener("click", downloadCsvReport);
    renderSamples();
    renderSummary();
  </script>
</body>
</html>
```

- [ ] **Step 2: Run static contract tests**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_local_dependencies_only tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_declares_required_sample_matrix tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_excludes_sensitive_media tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_tuning_recommendation_rules -v
```

Expected:

```text
OK
```

- [ ] **Step 3: Check HTML external dependencies**

Run:

```powershell
Select-String -Path "H:\AI_test\face_api\acceptance.html" -Pattern "cdn|script src|link rel=.*stylesheet|import |require\(|https?://"
```

Expected:

- No external CDN/script/style dependency.
- The default `http://localhost:8000` value may match `http://`; that is acceptable and should be noted if output appears.

- [ ] **Step 4: Record page shell checkpoint**

Run:

```powershell
git status --short -- acceptance.html tests/test_scripts_smoke.py
```

Expected:

- `acceptance.html` and `tests/test_scripts_smoke.py` are modified/new.
- Do not commit yet unless the user explicitly asks.

---

### Task 4: Implement Camera Capture And Existing API Workflow

**Files:**
- Modify: `acceptance.html`
- Modify: `tests/test_scripts_smoke.py`

- [ ] **Step 1: Add static test for complete API workflow helpers**

Add to `tests/test_scripts_smoke.py`:

```python
    def test_acceptance_page_implements_complete_login_workflow(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        for symbol in [
            "checkService",
            "startCamera",
            "captureBase64",
            "captureFrames",
            "createChallenge",
            "submitChallenge",
            "registerTestUser",
            "faceLogin",
            "runSampleAttempt",
        ]:
            self.assertIn(f"function {symbol}", html)
        self.assertIn("X-API-Key", html)
        self.assertIn("purpose = \"login\"", html)
        self.assertIn("createChallenge(\"register\")", html)
        self.assertIn("state.serviceStatus?.liveness?.register_enabled", html)
        self.assertIn("if (liveness.passed === false)", html)
        self.assertIn("Number.parseInt", html)
        self.assertIn("challenge_id: challenge.challenge_id", html)
```

- [ ] **Step 2: Run the new workflow test and verify failure**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_implements_complete_login_workflow -v
```

Expected before implementation:

```text
FAIL
```

- [ ] **Step 3: Add API request helper**

In `acceptance.html`, add this function inside `<script>`:

```javascript
async function faceApi(path, options = {}) {
  readConfig();
  const headers = Object.assign({ "Content-Type": "application/json" }, options.headers || {});
  if (state.apiKey) {
    headers["X-API-Key"] = state.apiKey;
  }
  const response = await fetch(`${state.apiBaseUrl}${path}`, Object.assign({}, options, { headers }));
  const text = await response.text();
  let data = {};
  try {
    data = text ? JSON.parse(text) : {};
  } catch (_) {
    data = { message: text };
  }
  if (!response.ok) {
    const detail = data.detail || {};
    const message = detail.reason || detail.message || data.message || response.statusText;
    throw new Error(`${detail.code || response.status}: ${message}`);
  }
  return data;
}
```

- [ ] **Step 4: Add service check and camera helpers**

Add:

```javascript
async function checkService() {
  try {
    const health = await fetch(`${$("apiBaseUrl").value.replace(/\/+$/, "")}/health`);
    if (!health.ok) throw new Error(`health ${health.status}`);
    const status = await faceApi("/system/status", { method: "GET" });
    state.serviceStatus = status;
    const registerLiveness = status.liveness?.register_enabled ? "开启" : "关闭";
    setText("serviceStatus", `服务正常；推理设备 ${status.device || "-"}；人脸数 ${status.faces_count ?? 0}；注册活体 ${registerLiveness}`);
    return status;
  } catch (error) {
    setText("serviceStatus", `服务检查失败：${error.message}`);
    throw error;
  }
}

async function ensureServiceStatus() {
  return state.serviceStatus || checkService();
}

async function startCamera() {
  if (state.stream) {
    state.stream.getTracks().forEach(track => track.stop());
  }
  state.stream = await navigator.mediaDevices.getUserMedia({ video: { width: 640, height: 480 }, audio: false });
  $("preview").srcObject = state.stream;
  setText("cameraStatus", "摄像头已打开，请保持脸部位于画面中央。");
}

async function ensureCamera() {
  if (!state.stream) {
    await startCamera();
  }
}

function captureBase64() {
  const video = $("preview");
  const canvas = $("captureCanvas");
  canvas.width = video.videoWidth || 640;
  canvas.height = video.videoHeight || 480;
  canvas.getContext("2d").drawImage(video, 0, 0, canvas.width, canvas.height);
  return canvas.toDataURL("image/jpeg", 0.86);
}

async function captureFrames(count = 24, intervalMs = 180) {
  await ensureCamera();
  const frames = [];
  for (let i = 0; i < count; i += 1) {
    frames.push(captureBase64());
    await new Promise(resolve => setTimeout(resolve, intervalMs));
  }
  return frames;
}
```

- [ ] **Step 5: Add challenge, register, and login workflow helpers**

Add:

```javascript
async function createChallenge(purpose = "login") {
  return faceApi("/liveness/challenges", {
    method: "POST",
    body: JSON.stringify({ purpose, terminal_id: "acceptance-console", action: "blink" })
  });
}

async function submitChallenge(challenge, frames) {
  return faceApi("/liveness/challenges/submit", {
    method: "POST",
    body: JSON.stringify({
      challenge_id: challenge.challenge_id,
      purpose: challenge.purpose,
      terminal_id: "acceptance-console",
      frames
    })
  });
}

async function registerTestUser() {
  readConfig();
  await ensureCamera();
  await ensureServiceStatus();
  let challengeId = null;
  if (state.serviceStatus?.liveness?.register_enabled) {
    const challenge = await createChallenge("register");
    const frames = await captureFrames(24, 180);
    const liveness = await submitChallenge(challenge, frames);
    if (liveness.passed === false) {
      throw new Error(liveness.reason || liveness.result_reason || "注册活体未通过");
    }
    challengeId = liveness.challenge_id;
  }
  const image = captureBase64();
  const result = await faceApi("/faces/register", {
    method: "POST",
    body: JSON.stringify({
      user_id: state.testUser.user_id,
      username: state.testUser.username,
      image,
      terminal_id: "acceptance-console",
      challenge_id: challengeId,
      metadata: { source: "acceptance.html", version: "V2.2" }
    })
  });
  setText("serviceStatus", `测试用户已注册/重绑：${result.user_id || state.testUser.user_id}`);
  return result;
}

async function faceLogin(challenge) {
  return faceApi("/auth/face-login", {
    method: "POST",
    body: JSON.stringify({
      image: captureBase64(),
      terminal_id: "acceptance-console",
      challenge_id: challenge.challenge_id,
      state: "v2.2-acceptance"
    })
  });
}
```

- [ ] **Step 6: Replace sample attempt stub with real workflow**

Replace `runSampleAttempt` with:

```javascript
async function runSampleAttempt(sampleId) {
  const sample = state.samples.find(item => item.id === sampleId);
  if (!sample || sample.attempts.length >= state.attemptTarget) return;
  try {
    setText("cameraStatus", `正在采集：${sample.label}`);
    const challenge = await createChallenge();
    const frames = await captureFrames(24, 180);
    const liveness = await submitChallenge(challenge, frames);
    if (liveness.passed === false) {
      sample.attempts.push(normalizeAttempt({
        authenticated: false,
        message: liveness.reason || liveness.result_reason || liveness.message || "活体挑战未通过",
        anti_spoof_risk: liveness.anti_spoof_risk || { level: "unknown", reasons: ["liveness_failed"] },
        quality_metrics: {}
      }));
      setText("cameraStatus", `${sample.label} 活体未通过，已记录结果。`);
      renderSamples();
      renderSummary();
      return;
    }
    const login = await faceLogin(challenge);
    sample.attempts.push(normalizeAttempt(Object.assign({}, login, {
      anti_spoof_risk: login.anti_spoof_risk || liveness.anti_spoof_risk,
      message: login.message || liveness.reason || "登录成功"
    })));
    setText("cameraStatus", `${sample.label} 采集完成。`);
  } catch (error) {
    sample.attempts.push(normalizeAttempt({
      authenticated: false,
      message: error.message,
      anti_spoof_risk: { level: "unknown", reasons: ["request_failed"] }
    }));
    setText("cameraStatus", `${sample.label} 采集失败：${error.message}`);
  }
  renderSamples();
  renderSummary();
}
```

- [ ] **Step 7: Wire buttons**

Add:

```javascript
$("checkServiceBtn").addEventListener("click", checkService);
$("startCameraBtn").addEventListener("click", startCamera);
$("captureStillBtn").addEventListener("click", async () => {
  try {
    await ensureCamera();
    captureBase64();
    setText("cameraStatus", "截图测试完成，未保存图片。");
  } catch (error) {
    setText("cameraStatus", `截图测试失败：${error.message}`);
  }
});
$("registerUserBtn").addEventListener("click", async () => {
  try {
    await registerTestUser();
  } catch (error) {
    setText("serviceStatus", `测试用户注册/重绑失败：${error.message}`);
  }
});
```

- [ ] **Step 8: Run workflow tests**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_implements_complete_login_workflow tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_local_dependencies_only tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_excludes_sensitive_media -v
```

Expected:

```text
OK
```

- [ ] **Step 9: Record workflow implementation checkpoint**

Run:

```powershell
git status --short -- acceptance.html tests/test_scripts_smoke.py
```

Expected:

- Workflow changes are present in `acceptance.html`.
- Do not commit yet unless the user explicitly asks.

---

### Task 5: Implement Result Summary, Report Safety, And Tuning Guidance

**Files:**
- Modify: `acceptance.html`
- Modify: `tests/test_scripts_smoke.py`

- [ ] **Step 1: Add richer static test for report fields**

Add to `tests/test_scripts_smoke.py`:

```python
    def test_acceptance_page_report_contains_required_fields(self):
        html = (ROOT / "acceptance.html").read_text(encoding="utf-8")

        for field in [
            "generated_at",
            "api_base_url",
            "test_user",
            "sample_type",
            "success_count",
            "failure_count",
            "low_count",
            "medium_count",
            "high_count",
            "accepted",
            "det_score",
            "brightness",
            "sharpness",
            "face_pixels",
        ]:
            self.assertIn(field, html)
        self.assertIn("样例类型,第几次,是否成功,风险等级,相似度,det_score,brightness,sharpness,face_pixels,中文原因,风险原因", html)
```

- [ ] **Step 2: Run test and verify failure if fields are incomplete**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_contains_required_fields -v
```

Expected:

- Fails if required fields are missing.
- Passes if Task 3 already included all strings; if it passes, keep the test as coverage and continue.

- [ ] **Step 3: Normalize quality metrics consistently**

Update `normalizeAttempt`:

```javascript
function normalizeQuality(result) {
  const quality = result.quality_metrics || result.face?.quality_metrics || result.match?.quality_metrics || {};
  return {
    det_score: quality.det_score ?? "",
    brightness: quality.brightness ?? "",
    sharpness: quality.sharpness ?? "",
    face_pixels: quality.face_pixels ?? ""
  };
}

function normalizeAttempt(result) {
  const risk = result.anti_spoof_risk || {};
  return {
    success: Boolean(result.authenticated || result.passed),
    similarity: result.similarity ?? result.match?.similarity ?? "",
    risk_level: risk.level || "unknown",
    risk_reasons: risk.reasons || [],
    quality: normalizeQuality(result),
    message: result.message || result.reason || result.detail?.reason || "无中文原因",
    apiKey: undefined,
    image: undefined,
    frames: undefined
  };
}
```

- [ ] **Step 4: Render a readable result table**

Replace `renderSummary` with:

```javascript
function renderSummary() {
  const rows = state.samples.map(sample => {
    const summary = summarizeSample(sample);
    return `
      <tr>
        <td>${sample.label}</td>
        <td>${summary.success_count}/${state.attemptTarget}</td>
        <td><span class="status low">${summary.low_count}</span> / <span class="status medium">${summary.medium_count}</span> / <span class="status high">${summary.high_count}</span></td>
        <td>${summary.accepted ? "符合预期" : "需复核"}</td>
        <td>${sample.attempts.at(-1)?.message || "未采集"}</td>
      </tr>
    `;
  }).join("");
  $("summary").innerHTML = `
    <table class="result-table">
      <thead><tr><th>样例</th><th>成功</th><th>低/中/高风险</th><th>验收结论</th><th>最近原因</th></tr></thead>
      <tbody>${rows}</tbody>
    </table>
  `;
  renderRecommendations();
}
```

- [ ] **Step 5: Make recommendation rules data-driven**

Add:

```javascript
function renderRecommendations() {
  const recommendations = buildRecommendations();
  const basic = recommendations.filter(item => item.audience === "小白建议");
  const advanced = recommendations.filter(item => item.audience === "开发/运维阈值方向");
  $("recommendations").innerHTML = `
    <h3>小白建议</h3>
    ${basic.map(item => `<p>${item.text}</p>`).join("")}
    <details>
      <summary>开发/运维阈值方向</summary>
      ${advanced.map(item => `<p>${item.text}</p>`).join("")}
    </details>
  `;
}
```

- [ ] **Step 6: Preserve report safety**

Confirm `buildReport()` ends with:

```javascript
apiKey: undefined,
image: undefined,
frames: undefined
```

Confirm no use of:

```javascript
localStorage
sessionStorage
indexedDB
```

- [ ] **Step 7: Run report tests**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_contains_required_fields tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_report_excludes_sensitive_media tests.test_scripts_smoke.ScriptSmokeTests.test_acceptance_page_has_tuning_recommendation_rules -v
```

Expected:

```text
OK
```

- [ ] **Step 8: Record report and recommendation checkpoint**

Run:

```powershell
git status --short -- acceptance.html tests/test_scripts_smoke.py
```

Expected:

- Report and recommendation changes are present.
- Do not commit yet unless the user explicitly asks.

---

### Task 6: Sync Architecture And User Documentation

**Files:**
- Modify: `architecture.html`
- Modify: `README.md`
- Modify: `docs/04_usage/03_recognition_security_accuracy.md`
- Modify: `tests/test_scripts_smoke.py`

- [ ] **Step 1: Add static tests for architecture and docs sync**

Add to `tests/test_scripts_smoke.py`:

```python
    def test_v22_docs_and_architecture_reference_acceptance_console(self):
        architecture = (ROOT / "architecture.html").read_text(encoding="utf-8")
        readme = (ROOT / "README.md").read_text(encoding="utf-8")
        security_doc = (ROOT / "docs" / "04_usage" / "03_recognition_security_accuracy.md").read_text(
            encoding="utf-8"
        )
        acceptance_record = (
            ROOT / "docs" / "90_archive" / "04_acceptance" / "06_v2.2_acceptance_record.md"
        ).read_text(encoding="utf-8")

        for text in (architecture, readme, security_doc, acceptance_record):
            self.assertIn("acceptance.html", text)
        self.assertIn("现场算法验收", architecture)
        self.assertIn("JSON/CSV", security_doc)
        self.assertIn("真人正脸", acceptance_record)
        self.assertIn("手机播放眨眼视频", acceptance_record)
```

- [ ] **Step 2: Run the new sync test and verify failure**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_v22_docs_and_architecture_reference_acceptance_console -v
```

Expected before docs sync:

```text
FAIL
```

- [ ] **Step 3: Update `architecture.html`**

Add `acceptance.html` to the visual architecture page in the local pages or frontend node list.

Use display text:

```text
acceptance.html：现场算法验收、五类样例、JSON/CSV 报告、调参建议
```

Add a flow note near liveness/anti-spoofing:

```text
V2.2 验收流程：摄像头预览 -> 测试用户注册/重绑 -> 五类样例各 3 次 -> 完整 face login -> JSON/CSV 报告 -> 保守调参建议
```

Do not add external dependencies.

- [ ] **Step 4: Update README entry points**

Modify `README.md` local page section with:

```markdown
- `acceptance.html`：V2.2 现场算法验收台，用于真人、打印照片、手机屏幕、电脑屏幕和播放视频样例验收，支持 JSON/CSV 报告和保守调参建议。
```

- [ ] **Step 5: Update recognition security document**

Modify `docs/04_usage/03_recognition_security_accuracy.md` with:

```markdown
## 7. V2.2 现场算法验收

V2.2 新增 `acceptance.html` 作为现场算法验收台。它使用同一个已注册测试用户，固定验证真人正脸、打印照片、手机屏幕照片、电脑屏幕照片和手机播放眨眼视频五类样例，每类默认采集 3 次。

页面会走完整登录链路，并在本地生成 JSON/CSV 报告。报告只包含结果、相似度、风险等级、中文原因和关键质量指标，不包含 API Key、原图、连续帧或 embedding。

调参建议分两层：默认给现场人员检查光线、距离、摄像头角度和样例一致性；展开后给开发/运维关注 `FACE_LIVENESS_MIN_BRIGHTNESS_VARIATION`、`FACE_ANTI_SPOOF_MIN_FRAME_DELTA`、`FACE_ANTI_SPOOF_MIN_FACE_MOTION`、`FACE_ANTI_SPOOF_MIN_SHARPNESS_VARIATION` 等阈值方向。

页面建议只用于本地工作站或受控内网验收。普通互联网业务前端仍不应直接持有 `X-API-Key`。
```

- [ ] **Step 6: Run sync tests**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke.ScriptSmokeTests.test_v22_docs_and_architecture_reference_acceptance_console -v
Select-String -Path "H:\AI_test\face_api\architecture.html","H:\AI_test\face_api\acceptance.html" -Pattern "cdn|script src|link rel=.*stylesheet|import |require\("
git diff --check -- architecture.html README.md docs\04_usage\03_recognition_security_accuracy.md tests\test_scripts_smoke.py
```

Expected:

- Sync test passes.
- No external dependency is introduced.
- `git diff --check` has no output.

- [ ] **Step 7: Record docs and architecture checkpoint**

Run:

```powershell
git status --short -- architecture.html README.md docs/04_usage/03_recognition_security_accuracy.md tests/test_scripts_smoke.py
```

Expected:

- Docs and architecture changes are visible.
- Do not commit yet unless the user explicitly asks.

---

### Task 7: Manual Browser Verification And Final Acceptance Record

**Files:**
- Modify: `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md`

- [ ] **Step 1: Run full automated verification**

Run:

```powershell
D:\anaconda3\envs\face_api\python.exe -m unittest discover -s tests -v
D:\anaconda3\envs\face_api\python.exe -m compileall -q main.py app_config.py business_demo tests scripts
git diff --check
```

Expected:

- Full unittest passes.
- `compileall` exits 0.
- `git diff --check` has no output.

- [ ] **Step 2: Run HTML dependency scan**

Run:

```powershell
Select-String -Path "H:\AI_test\face_api\architecture.html","H:\AI_test\face_api\camera-integration.html","H:\AI_test\face_api\admin.html","H:\AI_test\face_api\acceptance.html" -Pattern "cdn|script src|link rel=.*stylesheet|import |require\("
```

Expected:

- No external CDN, script, stylesheet, import, or require dependency.

- [ ] **Step 3: Prepare CORS for browser smoke**

If `FACE_ENV=production` or `run-prod.bat` is used, configure the acceptance page origin before starting `face_api`:

```powershell
$env:FACE_CORS_ORIGINS="http://localhost:8122"
```

If the API is already running in development mode with `FACE_CORS_ORIGINS=*`, this extra setting is not required.

Expected:

- Browser requests from `http://localhost:8122/acceptance.html` can send `X-API-Key`.
- CORS is treated as browser access control only; it does not replace `X-API-Key`.

- [ ] **Step 4: Open the page for manual smoke**

If `file:///` camera access is restricted by the browser, use the project server or a simple local static server. First try:

```powershell
Start-Process "H:\AI_test\face_api\acceptance.html"
```

If camera permission does not work, start a local static server:

```powershell
$staticJob = Start-Job -ScriptBlock {
  Set-Location "H:\AI_test\face_api"
  D:\anaconda3\envs\face_api\python.exe -m http.server 8122
}
```

Then open:

```text
http://localhost:8122/acceptance.html
```

Manual checks:

- 摄像头预览可见。
- API 地址和 API Key 输入框可用。
- 页面没有显示 API Key 明文在结果区域。
- 五类样例都可见。
- 下载 JSON/CSV 按钮可见。
- 小白建议和开发/运维阈值方向可见。

After manual smoke, stop the temporary static server:

```powershell
Stop-Job $staticJob
Remove-Job $staticJob
```

- [ ] **Step 5: 浏览器级 UI 验证**

Use the in-app Browser or another browser automation tool to open:

```text
http://localhost:8122/acceptance.html
```

Verify:

- Page title contains `face_api V2.2 现场算法验收`.
- Browser console has no JavaScript syntax/runtime errors on load.
- Buttons exist: `检查服务`、`打开摄像头`、`注册/重绑测试用户`、`下载 JSON 报告`、`下载 CSV 报告`。
- The five sample cards are visible.

- [ ] **Step 6: Optional live API smoke**

If `face_api` is running with `FACE_API_KEY`, do:

```powershell
$env:FACE_API_KEY="your-secret"
.\run.bat
```

In `acceptance.html`:

- 填写 `http://localhost:8000`。
- 填写同一个 API Key。
- 点击检查服务。
- 打开摄像头。
- 注册/重绑测试用户。
- 对真人正脸采集 1 次。

Expected:

- 服务检查成功。
- 注册/重绑成功或返回可读中文错误。
- 真人样例至少产生一条结果记录。

- [ ] **Step 7: Update acceptance record with verification results**

Modify `docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md` and replace `待执行` cells with actual results. Example:

```markdown
| 静态 smoke | `D:\anaconda3\envs\face_api\python.exe -m unittest tests.test_scripts_smoke -v` | 通过，N 个测试 OK |
| 全量 unittest | `D:\anaconda3\envs\face_api\python.exe -m unittest discover -s tests -v` | 通过，N 个测试 OK |
| 语法编译 | `D:\anaconda3\envs\face_api\python.exe -m compileall -q main.py app_config.py business_demo tests scripts` | 通过 |
| diff 检查 | `git diff --check` | 通过 |
```

For manual smoke, add:

```markdown
## 页面手动冒烟

| 检查项 | 结果 | 备注 |
|---|---|---|
| 页面打开 | 待记录 | `acceptance.html` |
| 摄像头预览 | 待记录 | 现场浏览器权限 |
| 服务检查 | 待记录 | 需要 face_api 运行 |
| JSON/CSV 下载 | 待记录 | 不包含 API Key、原图或连续帧 |
```

- [ ] **Step 8: Record final acceptance checkpoint**

Run:

```powershell
git status --short -- docs/90_archive/04_acceptance/06_v2.2_acceptance_record.md
```

Expected:

- Acceptance record contains the real verification result.
- Do not commit automatically; include one final local commit command in the version-complete summary.

- [ ] **Step 9: Final status check**

Run:

```powershell
git status --short --branch
git log --oneline -8
```

Expected:

- Working tree contains only the intended V2.2 files plus any pre-existing V2.1 follow-up files that were intentionally left uncommitted.
- Final summary includes grouped local commit commands and explains which files belong to V2.1 follow-up versus V2.2.

## Self-Review Checklist

- Spec coverage: tasks cover spec-kit baseline, standalone `acceptance.html`, five sample types, three attempts per type, complete login workflow, JSON/CSV reports, privacy constraints, tuning guidance, architecture sync, and acceptance record.
- Placeholder scan: no placeholder markers or ambiguous “fill in” instructions remain in this plan.
- Type and name consistency: plan consistently uses `acceptance.html`, `buildReport`, `downloadJsonReport`, `downloadCsvReport`, `captureFrames`, `createChallenge`, `submitChallenge`, `faceLogin`, and `023-field-algorithm-acceptance-console`.
- API compatibility: no task adds a backend endpoint, database table, or public API field.
- Safety: report generation explicitly excludes API Key, original image, continuous frames, and embedding.
